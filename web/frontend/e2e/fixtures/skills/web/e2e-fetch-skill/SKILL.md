---
name: e2e-fetch-skill
description: Fixture skill in the web category, used by Skills-gallery specs to verify category switching reveals a different set of entries.
---

# E2E Fetch Skill

Fixture content only — never mounted into a real task.

The fence below exists so the Resources spec can assert that a code block in a
skill's documentation is syntax highlighted by rehype-highlight in a real
browser, rather than only in the component tests.

```ts
const answer: number = 42;
```
