/**
 * The active row/tab treatment, shared by every nav list that used to
 * hand-write `bg-primary/10 text-primary` (App sidebar, WorkspaceSwitcher,
 * terminal sidebar, LaunchPad tabs, MCP sidebar, ConfigHub sections,
 * SectionLayout tabs).
 */
export const ACTIVE_CHIP = 'bg-primary/10 text-primary';
